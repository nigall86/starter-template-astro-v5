---
// styles
import '../styles/components/nav.css';

// components
import Link from '@components/ui/links/Link.astro';

// data
import { navLinks } from '@data/navData';
---

<nav class='site-nav' aria-label='primary navigation'>
  <ul class='site-nav__list' data-visible='false' role='list'>
    {
      navLinks.map((item) => (
        <li>
          <Link text={item.name} href={item.path} data-navlink />
        </li>
      ))
    }
  </ul>
</nav>

<Link
  type='button'
  style='btn-icon'
  classes='mobile-nav-toggle'
  aria-controls='navigation'
  aria-expanded='false'
>
  <svg
    role='img'
    class='mobile-nav-toggle__icon'
    style='--stroke-color: var(--text-1)'
  >
    <use id='nav-icon' href='#icon-menu'></use>
  </svg>
  <span class='sr-only'>Menu</span>
</Link>

<script>
  import '@scripts/nav.js';
</script>
